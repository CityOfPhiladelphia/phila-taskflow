#!/usr/bin/env python

from distutils.core import setup

setup(
    name='phila_taskflow',
    version='0.1dev',
    packages=['phila_taskflow',],
    install_requires=[
    ],
    dependency_links=[
    ],
)
